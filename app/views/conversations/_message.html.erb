<%= form_for(:conversations, url: conversations_message_path, remote: true) do |f| %>
	<%= f.label "Message:" %>
	<%= f.text_area :text, class: "new-message-field" %>
	<%= f.submit "Send" %>
	<%= f.hidden_field :conversation_id, :value => @conversation.id %>
<% end %>