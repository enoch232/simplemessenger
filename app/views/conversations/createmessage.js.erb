//if the message is same as last time, dont post agiain.
<% if @newmessage && @newmessage.last.text != $same %>
	$('.message-holder').append("<%= j render 'message_display', message: @newmessage.last %>");
	console.log("new messagepost!");
	<% $same = @newmessage.last.text %>
<% end%>